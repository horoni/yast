# yet another steganography tool
## Installation
Compile with following command
```
zig build -Doptimize ReleaseFast
```
then copy binaries
```
cp zig-out/bin/* /usr/local/bin/
```

## History
 - -> v3: rewrite from C to Zig
 - v3: written in C
 - v2: bash with python
 - v1: was written in bash
